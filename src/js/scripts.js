(function ($) {
  'use strict';

  /* ---------- ready starts ---------- */
  $(document).ready(function(e){
    
  });
  /* ---------- ready starts ---------- */

  /* ---------- load starts ---------- */
  $(window).on('load', function(e){

  });
  /* ---------- load ends ---------- */
      
})(jQuery);
    